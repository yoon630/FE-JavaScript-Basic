/* 3번 문제 답안을 작성해주세요. */
function solution(arr) {
  var answer = [];
  if(arr.length =0){
      answer = [-1];
      return answer;
  }
  else if(arr.length > 0){
    for(let i=0;i<arr.length;i++){
        
    }
     anwer=arr.splice(작은거 잘라내는거)
  }
  return answer;
}